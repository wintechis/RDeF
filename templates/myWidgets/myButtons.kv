<SelectButton>:
    padding_x: 10
    padding_y: 50
    #text: 'Select File'
    halign: 'left'
    valign: 'center'
    size_hint_x: None
    #width: min(self.parent.width, self.font_size/1.8 *len(self.text))
    text_size: self.width, None